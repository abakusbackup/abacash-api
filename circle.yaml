machine:
  node:
    version: 6.7.0

dependencies:
  pre:
    - npm install -g yarnpkg
  override:
    - yarn

test:
  override:
    - PG_URL='postgres://root@localhost/abacash-test' yarn test -- --timeout 0
    - yarn run lint
