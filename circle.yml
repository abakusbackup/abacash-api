machine:
  node:
    version: 6.7.0
  environment:
    NODE_ENV: test

dependencies:
  pre:
    - npm install -g yarnpkg
  override:
    - yarn

test:
  pre:
    - createdb abacash-test
  override:
    - yarn test -- --timeout 0
    - yarn run linte
